"""Phase 0 manual exercise utilities.

These functions are intentionally left for the learner to implement.
"""

from __future__ import annotations


def chunk_list(items: list[int], size: int) -> list[list[int]]:
    """Split a list into chunks of at most ``size`` items.

    Manual exercise boundary:
    - Raise ``ValueError`` when ``size`` is not positive.
    - Return a new list of list chunks.
    """
    raise NotImplementedError("Manual exercise: implement chunk_list")


def count_vowels(text: str) -> int:
    """Count vowels in the provided string.

    Manual exercise boundary:
    - Treat standard English vowels as vowels.
    - Return the total count as an integer.
    """
    raise NotImplementedError("Manual exercise: implement count_vowels")
